<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tiankong.mapper.UserMapper">
    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            user_name,
            user_account,
            user_password,
            user_role,
            <if test="userAvatar != null and userAvatar != ''">user_avatar,</if>
            <if test="gender != null">gender,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{userName},
            #{userAccount},
            #{userPassword},
            #{userRole},
            <!-- 处理可选字段 -->
            <if test="userAvatar != null and userAvatar != ''">#{userAvatar},</if>
            <if test="gender != null">#{gender},</if>
        </trim>
    </insert>

    <update id="resetPassword">
        UPDATE user
        SET user_password = 'a66abb5684c45962d887564f08346e8d'
        WHERE user_account = #{userId}
    </update>

    <select id="login" resultType="com.tiankong.pojo.User">
        SELECT
            id,
            user_name AS userName,
            user_account AS userAccount,
            user_avatar AS userAvatar,
            gender,
            user_role AS userRole,
            user_password AS userPassword,
            organize_id AS organizeId,
            create_time AS createTime,
            update_time AS updateTime,
            is_delete AS isDelete
        FROM user
        WHERE user_account = #{userAccount}
            AND user_password = #{userPassword}
            AND is_delete = 0   <!-- 只查询未删除用户 -->
            AND user_role IN (0, 1)  <!-- 限制有效角色类型 -->
            LIMIT 1  <!-- 确保唯一性 -->
    </select>

</mapper>